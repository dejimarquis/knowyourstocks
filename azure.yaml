name: knowyourstocks
metadata:
  template: azd-init

services:
  web:
    project: .
    language: js
    host: staticwebapp
    dist: dist

hooks:
  postprovision:
    shell: sh
    run: ./scripts/configure-intelligence-quota.sh
