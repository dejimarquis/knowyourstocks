{
  "name": "knowyourstocks-api",
  "version": "1.0.0",
  "private": true,
  "main": "dist/src/{index.js,functions/*.js}",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "func start --typescript --port 7072",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/data-tables": "^13.3.2",
    "@azure/functions": "^4.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
